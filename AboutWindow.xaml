<Window x:Class="animacja.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:animacja"
        mc:Ignorable="d"
        Title="About" Height="200" Width="300" ResizeMode="NoResize">
    <Grid VerticalAlignment="Top" Height="180">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label HorizontalAlignment="Center" Width="198" VerticalAlignment="Center" Height="74">
            <TextBlock HorizontalAlignment="Center" TextAlignment="Center" VerticalAlignment="Center">
                Application realised as a test
                <LineBreak/>recrutation task for Polixel Sp. z o.o.
                <LineBreak/>by Paweł Gomoliński
                <LineBreak/>in September of 2021.
            </TextBlock>
        </Label>

        <Button x:Name="AboutOK" Click="AboutOKClick" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="48" Cursor="Hand" IsCancel="True" IsDefault="True" Height="20">OK</Button>
    </Grid>
</Window>
